# Open Questions

Classified per Blueprint §21: BLOCKER / DECISION_REQUIRED / CONFIGURATION_DEFAULT / RESEARCH_REQUIRED / IMPLEMENTATION_DETAIL.

1. **[BLOCKER, Align integration only]** The identity of the "Align" CRM vendor is unconfirmed (most plausible candidate: Aline CRM, formed from the 2022 Enquire/Glennis/Sherpa merger — not verified with the actual vendor). No outbound webhook capability is documented. **Does not block** any other Phase 1–8 work; the integration adapter interface should be built now with a `pending` status and a manual-import fallback.
2. **[DECISION_REQUIRED — recommendation pending approval, 2026-09-26]** Recommended: shared schema, `tenant_id` on every tenant-owned row, composite `(tenant_id, id)` FKs, RLS with `FORCE`, app connects as a non-owner role without BYPASSRLS, tenant bound per transaction via `SET LOCAL app.tenant_id` (safe under PgBouncer transaction pooling; session-level `SET` is not). Row-level security connection strategy: session-variable-based (`SET app.tenant_id`) vs. per-tenant connection pooling vs. a schema-per-tenant approach. Affects the RLS policies stubbed in `docs/db-schema-phase1.sql`.
3. **[DECISION_REQUIRED]** Target payroll vendor(s) for the first integration (ADP / Gusto / Paychex / QuickBooks Payroll) — needed before writing the first real vendor adapter (the interface itself is vendor-agnostic and doesn't need this decision).
4. **[CONFIGURATION_DEFAULT]** GPS/location-tracking notice: given the NJ vehicle-tracking statute's exemption scope is uncertain (COMPLIANCE-ASSUMPTIONS #13), default to written notice for every employee whose location is captured for any purpose, not only mileage.
5. **[CONFIGURATION_DEFAULT]** Overage/entitlement policy defaults (pre-authorize vs. auto-authorize vs. absorb up to N hours), rollover policy, and documentation grace window are all engineer-settable defaults per the original Blueprint v2 — not re-litigated here.
6. **[RESEARCH_REQUIRED before Phase 11]** Rev. Rul. 99-7 commute-vs-business classification needs to be implemented as explicit logic (origin/destination type + "regular work location" attribute) before mileage reimbursement goes live for home-based staff — currently only the rate engine exists, not the commute/business classifier.
7. **[RESEARCH_REQUIRED before Phase 12]** Confirm actual behavior of the chosen payroll vendor's API (idempotency, append vs. overwrite, post-close correction path) in a sandbox before relying on any assumption in COMPLIANCE-ASSUMPTIONS #15.
8. **[IMPLEMENTATION_DETAIL]** Framework choice for the HTTP/API layer (Fastify vs. Express vs. Hono) — not yet selected; Phase 1 as built here has no HTTP layer, only domain logic and tests. See IMPLEMENTATION-DECISIONS.md #3.
9. **[DECISION_REQUIRED, 2026-09-26]** Repository strategy. `simensen8/Test` is **public** and also holds the working Adult Day billing-reconciliation app (Python) at the root. The platform was imported to `hcbs-platform/` provisionally. Recommended: move the platform to a **private** repository (or make this one private); keep the Python app running as-is (maintenance only) until a platform module supersedes it.
10. **[DECISION_REQUIRED, 2026-09-26]** Relationship between the Python app and the platform. Recommended: TypeScript stays the platform stack (IMPLEMENTATION-DECISIONS #1); no rewrite of the Python app now; its domain (attendance, payer rules, PCC batch reconciliation) becomes the reference spec for a later platform module and a data-migration source.
11. **[DECISION_REQUIRED — golden test change, 2026-09-26]** AUDIT D6: `NjPerDiemExemptionAttestation` lacks the statutory "as-needed to supplement or replace/substitute" criterion. Fixing it adds a required field, so Golden Test 5's fixtures must gain that field (assertions unchanged; both cases still expect `false`) plus a new positive/negative case. Needs approval under CLAUDE.md rule 2.
12. **[RESEARCH_REQUIRED]** NJ-specific salary threshold / exemption rules for EAP (N.J.A.C. 12:56) — not yet researched; federal floor only is modeled.
13. **[RESEARCH_REQUIRED]** Current codified text of N.J.S.A. 34:11D-1..-3 (post-2018 amendments, if any) — only the as-enacted chapter law was checked.
14. **[DECISION_REQUIRED before Phase 2]** Hosting under a BAA, identity provider (OIDC/MFA), and secrets manager — needed before any real PHI or payroll data reaches the platform.
15. **[RESEARCH_REQUIRED before scheduling/EVV]** MCO contract EVV and visit-verification terms for the org's payers; HHAeXchange aggregator interface for the services listed in COMPLIANCE-ASSUMPTIONS #12.
